# Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Asserts
        - \Helper\Unit
    step_decorators: ~
aspect_mock:
    include_paths:
        - src
        - public
    temp_folder:
        "unit_tests"